services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DATABASE: testdb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - 5432:5432
    volumes:
      - ./pg/init/:/docker-entrypoint-initdb.d/

